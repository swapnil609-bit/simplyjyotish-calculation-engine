# Extension guide

Add a domain as a small typed module with a documented convention, explicit
settings, focused tests, a regression fixture, and a feature-matrix entry.
Reuse `core` time/provenance and `astronomy` position primitives. Do not add
network clients, prediction prose, or a second implementation of Swiss
Ephemeris calculations.

